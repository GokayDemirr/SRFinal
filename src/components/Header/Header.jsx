import ProfileCard from "./ProfileCard/ProfileCard";

const Header = () => {
  return (
    <header className="flex h-16 bg-neutral-200">
      <ProfileCard />
    </header>
  );
};

export default Header;
